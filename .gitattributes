# All files should use LF by default
*.*             text eol=lf

# Then we proceed to exclude specific files from using LF

# Linux start script should use LF
/gradlew        text eol=lf

# These are Windows script files and should use CRLF
*.bat           text eol=crlf

# Binary files should be left untouched
*.jar           binary
