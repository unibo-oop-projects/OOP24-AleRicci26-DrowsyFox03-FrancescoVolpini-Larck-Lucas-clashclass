package clashclass.ai.behaviourtree;

public class AttackBuildingNode extends AbstractBehaviourNode {
    // TODO: attackRate (stats della truppa)
    // TODO: timer che usa l'attackRate

    @Override
    public State onUpdate(float deltaTime) {
        return super.onUpdate(deltaTime);
    }
}
